apiVersion: v1
kind: Service
metadata:
  name: calibre-service
spec: 
  type: NodePort
  selector:
    app.kubernetes.io/name: calibre
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080