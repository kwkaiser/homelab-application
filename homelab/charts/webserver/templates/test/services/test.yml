apiVersion: v1
kind: Service
metadata:
  name: calibre-service
spec: 
  selector:
    app: calibre
    app.kubernetes.io/name: calibre
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080