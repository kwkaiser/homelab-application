server {
  listen 80;
  server_name auth.*;

  location / {
    include trusted-proxy.conf;
    proxy_pass http://authelia;
  }

  location /api/verify {
    include trusted-proxy.conf;
    proxy_pass https://authelia;
  }
}