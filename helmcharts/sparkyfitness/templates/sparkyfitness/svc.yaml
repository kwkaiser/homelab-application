apiVersion: v1
kind: Service
metadata:
  name: sparkyfitness
spec:
  selector:
    app: sparkyfitness
    app.kubernetes.io/name: sparkyfitness
  ports:
    - protocol: TCP
      name: sparkyfitness
      port: 80
      targetPort: 80
