apiVersion: v1
kind: Service
metadata:
  name: sparkyfitnessserver
spec:
  selector:
    app: sparkyfitnessserver
    app.kubernetes.io/name: sparkyfitnessserver
  ports:
    - protocol: TCP
      name: sparkyfitnessserver
      port: 3010
      targetPort: 3010
