apiVersion: v1
kind: Service
metadata:
  name: dawarich
spec:
  selector:
    app: dawarich
    app.kubernetes.io/name: dawarich
  ports:
    - protocol: TCP
      name: dawarich
      port: 80
      targetPort: 3000
